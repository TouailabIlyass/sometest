<!-- Header -->
<div class="header pb-8 pt-5 pt-lg-8 d-flex align-items-center" style="min-height: 100px; background-image: url(img/theme/profile-cover.jpg); 
background-size: cover; background-position: center top;">
  <!-- Mask -->
  <span class="mask bg-gradient-default opacity-8"></span>
  <!-- Header container -->
  <div class="container-fluid d-flex align-items-center">
    <div class="row">
      <!-- <div class="col-lg-11 col-md-10">
        <h3 class="display-5 text-white"> Nom de fichier : <%=  locals.fileInfo.file_name || 'File' %></h3>
        <p class="text-white mt-0 mb-5"> Description <%= locals.fileInfo.description   %></p>
        <p class="success alert-primary" role="alert">Signé le : <%= locals.fileInfo.signed_at  || 'undefined' %></p>
        <p class="text-white mt-0 ">Signataire : <%= locals.fileInfo && locals.fileInfo.owner_id || 'undefined' %></p>
 
      </div> -->
    </div>
  </div>
</div>
